'use client';

// import { getSession } from 'next-auth/react';

export const UserPreview = async () => (
    // const {user} = await getSession();

    <>
        <h3>Nome: </h3>
        <p>Email: </p>
    </>
);
